# AI Spend Audit

Free web app for startups to audit AI tool spending and surface savings — built for [Credex](https://credex.rocks).

## Quick start

```bash
npm install
cp .env.example .env.local
# Add Supabase, Anthropic, and Resend keys to .env.local
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

### Supabase

Run `supabase/schema.sql` in your Supabase SQL editor.

## Scripts

| Command | Description |
|---------|-------------|
| `npm run dev` | Local development |
| `npm run build` | Production build |
| `npm run lint` | ESLint |

## Stack

Next.js 14 · TypeScript · Tailwind · shadcn/ui · Supabase · Anthropic · Resend · Vercel

## Deployed URL

(Add after Vercel deploy)
